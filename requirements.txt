# =============================================================================
# requirements.txt  –  DeepGlobe Satellite Pipeline
# =============================================================================
# Covers all stages:
#   Stage 1 : Road Extraction       (DeepLabV3+ ResNet34)
#   Stage 2 : Map Inpainting        (Partial Conv U-Net)
#   Stage 3 : Land Cover Classification (DeepLabV3+ ResNet34, 7-class)
#   Stage 4 : Building Detection    (UnetPlusPlus ResNet50 + scse, 640×640)
#
# Install: pip install -r requirements.txt
#
# Environment tested on:
#   • Kaggle  T4 ×2  (recommended)
#   • Google Colab Free T4
# =============================================================================

# ── Core Deep Learning ────────────────────────────────────────────────────────
torch>=2.0.0                      # AMP (autocast + GradScaler), DataParallel
torchvision>=0.15.0               # VGG16 perceptual loss backbone (inpainting)

# ── Segmentation Models ───────────────────────────────────────────────────────
segmentation-models-pytorch>=0.3.3  # DeepLabV3+, U-Net, all encoder backbones
                                    # also provides smp.losses.DiceLoss

# ── Image I/O & Processing ───────────────────────────────────────────────────
opencv-python>=4.7.0              # cv2: imread, cvtColor, INTER_NEAREST, etc.
Pillow>=9.0.0                     # PIL: used in inpainting pipeline

# ── Augmentation ──────────────────────────────────────────────────────────────
albumentations>=1.3.0             # HueSaturationValue, CoarseDropout, GaussNoise
                                  # mask_interpolation param needs >= 1.3.0

# ── Numerical & Scientific ───────────────────────────────────────────────────
numpy>=1.23.0

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.6.0                 # All visualisation scripts
seaborn>=0.12.0                   # evaluate_landcover.py confusion matrix heatmap

# ── Progress & Logging ───────────────────────────────────────────────────────
tqdm>=4.64.0                      # Training progress bars

# ── Miscellaneous ─────────────────────────────────────────────────────────────
scikit-image>=0.19.0              # SSIM computation in evaluate_inpainting.py
scikit-learn>=1.1.0               # Metrics helpers